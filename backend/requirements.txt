# This is a reference-implementation + cross-validation toolkit now, not a
# deployed server -- see the top-level README. Only what app/blackbox,
# app/analysis, app/fc, and app/tuning actually import, plus pytest.
numpy>=1.26
scipy>=1.11
pyserial>=3.5

pytest>=8.0
